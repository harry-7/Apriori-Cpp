#include <bits/stdc++.h>
#include "trie.h"

using namespace std;

vector<int> frequencies;
vector<vector<int> > table;
set<int> uniq;
unordered_map<string, int> map1;
unordered_map<int, int> map2;
vector<int> single_item_sets;
vector< vector<int> > tri_array;
Trie tree;
vector<string> item_map;

void update_record(int record) {}

void increment_record(int record) {}

vector<vector<int> > apriori_gen(int k) {

}

int main() {
    return 0;
}